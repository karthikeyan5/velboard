{
  "id": "cpu",
  "contractVersion": "1.0",
  "name": "CPU Load",
  "description": "Real-time CPU usage with core count and progress bar",
  "version": "1.0.0",
  "author": "core",
  "position": 10,
  "size": "half",
  "refreshMs": 2000,
  "requires": [],
  "capabilities": ["fetch"],
  "dataSchema": {
    "type": "object",
    "properties": {
      "load": { "type": "number" },
      "cores": { "type": "integer" }
    },
    "required": ["load", "cores"]
  },
  "rateLimit": {
    "windowMs": 60000,
    "max": 30
  },
  "config": {}
}
