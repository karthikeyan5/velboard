{
  "id": "processes",
  "contractVersion": "1.0",
  "name": "Process Overview",
  "description": "Total, running, sleeping process counts and OS info",
  "version": "1.0.0",
  "author": "core",
  "position": 50,
  "size": "full",
  "refreshMs": 2000,
  "requires": [],
  "capabilities": ["fetch"],
  "dataSchema": {
    "type": "object",
    "properties": {
      "total": { "type": "integer" },
      "running": { "type": "integer" },
      "sleeping": { "type": "integer" },
      "os": { "type": "string" }
    },
    "required": ["total", "running", "sleeping"]
  },
  "rateLimit": {
    "windowMs": 60000,
    "max": 30
  },
  "config": {}
}
