{
  "id": "memory",
  "contractVersion": "1.0",
  "name": "Memory Usage",
  "description": "RAM usage with total/used breakdown and progress bar",
  "version": "1.0.0",
  "author": "core",
  "position": 20,
  "size": "half",
  "refreshMs": 2000,
  "requires": [],
  "capabilities": ["fetch"],
  "dataSchema": {
    "type": "object",
    "properties": {
      "total": { "type": "number" },
      "used": { "type": "number" },
      "free": { "type": "number" },
      "available": { "type": "number" },
      "pct": { "type": "number" }
    },
    "required": ["total", "used", "pct"]
  },
  "rateLimit": {
    "windowMs": 60000,
    "max": 30
  },
  "config": {}
}
