{
  "checks": [
    {
      "type": "http_get",
      "path": "/api/health",
      "expect_status": 200,
      "hint": "Velboard /api/health should always return 200."
    },
    {
      "type": "http_get",
      "path": "/api/sources",
      "expect_status": 401,
      "hint": "Velboard /api/sources requires auth — 401 without valid session is correct."
    },
    {
      "type": "file_exists",
      "path": "data",
      "relative_to": "app",
      "hint": "velboard/data directory missing. Panel data files may not load."
    }
  ]
}
